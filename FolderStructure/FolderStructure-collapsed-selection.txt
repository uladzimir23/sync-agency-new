АНАЛИЗ ПАПКИ: /Users/vladimirmazyrec/sync-agency-new/src/shared/ui/collapsed-selection
Python версия: 3.12.4
================================================================================

СТРУКТУРА ПАПОК (полные пути):
--------------------------------------------------------------------------------

collapsed-selection
    ├── collapsed-selection.module.scss
    ├── collapsed-selection.tsx
    └── index.ts


================================================================================
СОДЕРЖИМОЕ ФАЙЛОВ:
================================================================================

АБСОЛЮТНЫЙ ПУТЬ: /Users/vladimirmazyrec/sync-agency-new/src/shared/ui/collapsed-selection/collapsed-selection.module.scss
название файла: collapsed-selection/collapsed-selection.module.scss

СОДЕРЖИМОЕ ФАЙЛА:
----------------------------------------
.collapsedSelection {
  border-radius: 1.5rem;
  border: 1px solid var(--border-color);
  background: var(--bg-primary);
  transition: all 1s ease;
  overflow: hidden;
  backdrop-filter: blur(20px);
  display: flex;
  flex-direction: column;
  //min-height: 75px;
  position: relative;


  &:hover {
    transition: all 0.2s ease;
    box-shadow: var(--box-shadow-primary);

  }



  &.expanded {
    background: var(--bg-secondary);
    min-height: auto;
    display: flex;
    flex-direction: column;
    border-radius: 2rem;
    transition: all 1s ease;
    position: relative;




    &:hover {
      transition: all 0.2s ease;
      box-shadow: var(--box-shadow-primary);

      .header {
        //background: var(--bg-secondary);

      }

    }
  }


  .header {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    //gap: 12px;
    padding: 16px 16px 16px 24px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    width: 100%;

    &:hover {
      background: var(--bg-secondary);

    }
  }

  .details {
    //flex: 1;
    //min-width: 0;
    //width: 100%;

  }

  .summary {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
    font-size: 1.2rem; // Обновленный размер шрифта
    font-weight: 500;
    color: var(--text-primary);
    width: 100%;
  }

  .month,
  .date,
  .time {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .separator {
    color: var(--text-secondary);
    font-size: 12px;
  }


  .arrowWrapper {
    position: relative;
    width: 40px;
    height: 40px;
    display: flex;
    align-content: center;
    justify-content: center;
    align-items: center;
    border-radius: 40%;
    box-shadow: var(--box-shadow-primary), inset 0px 0px 1px var(--border-color-accent);
    // Градиентная обводка
        &::before {
          content: '';
          position: absolute;
          inset: -1px;
          border-radius: 100%;
          padding: 1px;
          background: linear-gradient(
            135deg,
            var(--primary-color) 0%,
            var(--border-color-accent) 50%,
            var(--text-secondary) 100%
          );
          mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
          mask-composite: exclude;
          opacity: 0;
          transition: all 0.3s ease;
          border-radius: 40%;
  
        }
  
          &:hover {
            &::before {
              opacity: 1;
              transform: scale(0.98);
              border-radius: 50%;
              
        
            }
            
            .arrow {
              transform: scale(1.02), rotate(180deg);
            }
          }
        }

  .arrow {
    color: var(--text-secondary);
    font-size: 14px;
    transition: transform 0.3s ease;
    flex-shrink: 0;

  }

  &.expanded .arrow {
    transform: rotate(180deg);
  }

  // Анимация для контента
  .content {
    max-height: 0px;
    opacity: 0;
    overflow: hidden;
    transition: max-height 0.4s ease, opacity 0.5s ease, padding 0.3s ease, background 0.3s ease,box-shadow 0.3s ease, scale 0.3s ease;
    backdrop-filter: blur(20px);
    transform-origin: top;
    position: relative;
    
    * {
      transition: max-height 0.4s ease, opacity 0.5s ease, padding 0.3s ease, background 0.3s ease,box-shadow 0.3s ease, scale 0.3s ease;
      scale: 0;
      //transform: scale(0);
      //transform-origin: center center; /* Указывает центр трансформации внизу по центру */

    }
  
    
    &.expanded {
      position: relative;

      //max-height: auto;
      max-height: 475px;
      opacity: 1;
      padding: 25px 20px;
      margin: 0px 5px 5px 5px;
      backdrop-filter: blur(20px);
      background: var(--background-primary-yellow);
      border: 1px solid var(--border-color);
      gap: 16px;
      border-radius: 1.8rem;
      display: flex;
      flex-direction: column;
      transition: max-height 0.4s ease, opacity 0.5s ease, padding 0.3s ease, background 0.3s ease,box-shadow 0.3s ease, scale 0.3s ease;
      transform-origin: top;
      overflow-y: auto; /* Показывает вертикальную прокрутку только если контент больше 300px */



      * {
        transition: max-height 0.4s ease, opacity 0.5s ease, padding 0.3s ease, background 0.3s ease,box-shadow 0.3s ease, scale 0.3s ease;
        //transform: scale(1);
        //transform-origin: center center; /* Указывает центр трансформации внизу по центру */
        scale: 1;
      }
    

      &:hover {
        transition: all 0.3s ease;
        background: var(--bg-primary);
        box-shadow: var(--box-shadow-primary);
        border: 1px solid var(--border-color);

        
      }


      @media (min-width: 1400px) {
        
        //max-width: 430px;
        padding: 20px;
        height: 100%;

      }
      

    }
  }
}

.collapsedSelection.disabled {
  opacity: 0.6;
  cursor: not-allowed;
  
  .header:hover {
    background: var(--bg-primary);
  }
}


@media (max-width: 480px) {
  .collapsedSelection {
    .header {
      padding: 1rem 1.2rem;
      display: flex;
    }
    
    .summary {
      font-size: 1.2rem;
      gap: 6px;
    }
  }
}

________________________________________________________________________________

АБСОЛЮТНЫЙ ПУТЬ: /Users/vladimirmazyrec/sync-agency-new/src/shared/ui/collapsed-selection/collapsed-selection.tsx
название файла: collapsed-selection/collapsed-selection.tsx

СОДЕРЖИМОЕ ФАЙЛА:
----------------------------------------
import React from 'react'
import { FaChevronDown } from 'react-icons/fa'; // Импортируем иконку
import { BookingDate, TimeSlot } from '@shared/sections/booking-section/types.ts'
import styles from './collapsed-selection.module.scss'

interface CollapsedSelectionProps {
  selectedMonth: Date
  selectedDate: BookingDate | undefined
  selectedTime: TimeSlot | null
  formatMonth: (date: Date) => string
  formatDate: (date: Date) => string
  onExpand: () => void
  children?: React.ReactNode
  isExpanded?: boolean
  isDisabled?: boolean
  title?: string
  forceTitle?: boolean
}

export const CollapsedSelection: React.FC<CollapsedSelectionProps> = ({
  selectedMonth,
  selectedDate,
  selectedTime,
  formatMonth,
  formatDate,
  onExpand,
  children,
  isExpanded = false,
  isDisabled = false,
  title = 'Select Date & Time',
  forceTitle = false
}) => {
  const handleClick = () => {
    if (!isDisabled) {
      onExpand()
    }
  }

  return (
    <div 
      className={`${styles.collapsedSelection} ${isExpanded ? styles.expanded : ''} ${isDisabled ? styles.disabled : ''}`}
    >
      <div className={styles.header} onClick={handleClick}>
        <div className={styles.details}>
          <div className={styles.summary}>
            {forceTitle ? (
              <span>{title}</span>
            ) : selectedTime ? (
              <>
                <span className={styles.date}>{formatDate(selectedDate?.date || new Date())}</span>
                <span className={styles.separator}>•</span>
                <span className={styles.time}>{selectedTime.time}</span>
              </>
            ) : selectedDate ? (
              <>
                <span className={styles.month}>{formatMonth(selectedMonth)}</span>
                <span className={styles.separator}>•</span>
                <span className={styles.date}>{formatDate(selectedDate.date)}</span>
              </>
            ) : (
              <span>{title}</span>
            )}
          </div>
        </div>

      < div className={styles.arrowWrapper}>
       <FaChevronDown className={`${styles.arrow} ${isExpanded ? styles.expanded : ''}`} />
       </div>
    </div>

      <div className={`${styles.content} ${isExpanded ? styles.expanded : ''}`}>
        {children}
      </div>
    </div>
  )
}

________________________________________________________________________________

АБСОЛЮТНЫЙ ПУТЬ: /Users/vladimirmazyrec/sync-agency-new/src/shared/ui/collapsed-selection/index.ts
название файла: collapsed-selection/index.ts

СОДЕРЖИМОЕ ФАЙЛА:
----------------------------------------
export { CollapsedSelection } from './collapsed-selection'

================================================================================

СТАТИСТИКА:
----------------------------------------
Обработано файлов: 3
Общий размер папки: 7,762 байт
Дата создания отчета: 2026-01-19 06:49:05
