АНАЛИЗ ПАПКИ: /Users/vladimirmazyrec/sync-agency-new/src/shared/sections/ProductHeroSection
Python версия: 3.12.4
================================================================================

СТРУКТУРА ПАПОК (полные пути):
--------------------------------------------------------------------------------

ProductHeroSection
    ├── index.ts
    ├── ProductHeroSection.module.scss
    └── ProductHeroSection.tsx


================================================================================
СОДЕРЖИМОЕ ФАЙЛОВ:
================================================================================

АБСОЛЮТНЫЙ ПУТЬ: /Users/vladimirmazyrec/sync-agency-new/src/shared/sections/ProductHeroSection/index.ts
название файла: ProductHeroSection/index.ts

СОДЕРЖИМОЕ ФАЙЛА:
----------------------------------------
export { ProductHeroSection } from './ProductHeroSection'

________________________________________________________________________________

АБСОЛЮТНЫЙ ПУТЬ: /Users/vladimirmazyrec/sync-agency-new/src/shared/sections/ProductHeroSection/ProductHeroSection.module.scss
название файла: ProductHeroSection/ProductHeroSection.module.scss

СОДЕРЖИМОЕ ФАЙЛА:
----------------------------------------
.container {
  position: relative;
  border-radius: 45px;
  padding: 2.5rem;
  background: var(--bg-primary-opacity);
  border: 1px solid var(--border-color);
  transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  overflow: hidden;
  cursor: pointer;
  min-height: 320px;
  width: 100%;
  height: 100%;
  scale: 1;
  backdrop-filter: blur(2px) saturate(250%);
  -webkit-backdrop-filter: blur(2px) saturate(250%);
  
  &:hover, &[data-active="true"] {
    background: var(--bg-primary-opacity-sec);
    box-shadow: var(--box-shadow-primary), inset 0px 0px 50px var(--background-primary-yellow-100), 0 0px 30px rgba(12, 140, 233, 0.15);
    border-radius: 3.2rem;
    scale: 1.01;
    backdrop-filter: blur(3px) saturate(250%);
    -webkit-backdrop-filter: blur(3px) saturate(250%);
    

    
    .backgroundWrapper {
      scale: 1.05;
      transition: all 0.4s ease, filter 0.2s ease;

      filter: blur(3px);
    }
    
    .content {
      transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
      scale: 1.01;
    }
    
    .title {
      color: var(--text-primary);
      transform: translateY(-2px);
    }

    .ctaContainer {
      opacity: 1;
      transform: translateY(0);
    }

    .subtitle {
      color: var(--text-primary);
      background: var(--background-primary);
      border: 1px solid var(--border-color);
      box-shadow: var(--background-primary);
      transition: all 0.3s ease;
      z-index: 99;
    }
  }

  &::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(var(--primary-color-rgb), 0.1),
      transparent
    );
    transition: left 0.7s ease;
  }

  &:hover::before, &[data-active="true"]::before  {
    left: 100%;
  }
}

.backgroundWrapper {
  scale: 1;
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
  border-radius: inherit;
  overflow: visible;
  transition: all 0.4s ease;
}

.content {
  position: relative;
  z-index: 2;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  gap: 1.5rem;
  height: 100%;
  scale: 1;
  transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.header {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  position: static;
}

.headerTitle {
  display: flex;
  flex-direction: row;
  gap: 7px;
}

.arrowContainer {
  position: absolute;
  top: -1rem;
  right: -1rem;
  width: 2rem;
  height: 2rem;
  z-index: 3;
  opacity: 0;
  transform: scale(0.5) rotate(-45deg) translate(-10px, 0px);
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1) 0.1s;
  
  &.arrowVisible {
    opacity: 1;
    transform: scale(1) translate(0, 0) rotate(-45deg);
  }
}

.arrow {
  font-size: 0.8rem;
  background: linear-gradient(45deg, #ff7b0016, #df00f816, #00eeff1c);
  border-radius: 50px;
  padding: 0.4rem;
  width: 1.8rem;
  height: 1.8rem;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  color: var(--text-primary);
  border: 1px solid var(--border-color);
  display: flex;
  align-items: center;
  justify-content: center;
  backdrop-filter: blur(15px) saturate(250%);
  
  &:hover {
    transform: scale(1.1) rotate(0deg);
  }
}

.subtitle {
  position: relative;
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--text-secondary);
  letter-spacing: 0.05em;
  text-transform: uppercase;
  width: fit-content;
  display: inline-block;
  background: var(--background-primary-yellow-100);
  border: 1px solid var(--border-color);
  padding: 5px 10px;
  border-radius: 50px;
  backdrop-filter: blur(15px) saturate(280%);
  -webkit-backdrop-filter: blur(15px) saturate(280%);
  transition: all 0.3s ease;
  overflow: hidden;
  
  &.small {
    font-size: 0.75rem;
    padding: 3px 8px;
  }
  
  &.medium {
    font-size: 0.75rem;
    padding: 5px 10px;
  }
  
  &.large {
    font-size: 1rem;
    padding: 8px 16px;
  }
}

.title {
  font-size: 1.5rem;
  font-weight: 600;
  color: var(--text-primary);
  transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  line-height: 1.2;
}

.description {
  opacity: 0;
  font-size: 1rem;
  line-height: 1;
  color: var(--text-secondary);
  transform: translateY(10px);
  height: 0;
  overflow: hidden;
  transition: all 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
  display: flex;
  align-items: flex-end;
  transform-style: preserve-3d;
  
  &.descriptionVisible {
    color: var(--text-primary);
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
    line-height: 1.4;
    opacity: 1;
    transform: translateY(0) translateZ(0px);
    height: 100%;

    &::before {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 100%;
      //background:var(--border-color);
      z-index: -1;
      filter: blur(5px);
    }
  }
}

.ctaContainer {
  margin-top: auto;
  opacity: 0;
  transform: translateY(10px);
  transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1) 0.1s;
  
  &.ctaVisible {
    opacity: 1;
    transform: translateY(0);
  }
}

.ctaButton {
  width: fit-content;
  transition: all 0.3s ease;
  
  &:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(var(--primary-color-rgb), 0.3);
  }
}

@media (max-width: 1440px) {
  .container {
    min-height: 280px;
    padding: 2rem;
  }
  
  .title {
    font-size: 1.5rem;
  }
  
  .description {
    font-size: 1rem;
  }

  .arrowContainer {
    top: -0.5rem;
    right: -0.5rem;
  }
  

  .subtitle {
    font-size: 0.8rem;
    
    &.medium {
      font-size: 0.7rem;
      padding: 4px 8px;
    }
  }
}

@media (max-width: 1024px) {
  .container {
    padding: 2rem;
    min-height: 240px;
  }
  
  .title {
    font-size: 1.3rem;
  }
  
  .description {
    font-size: 0.9rem;
  }

  .arrowContainer {
    top: -0.5rem;
    right: -0.5rem;
  }
  
  
  .subtitle {
    font-size: 0.8rem;
    
    &.medium {
      font-size: 0.6rem;
      padding: 4px 8px;
    }
  }
}

@media (max-width: 480px) {
  .container {
    padding: 2rem;
    min-height: 220px;
    border-radius: 3rem;
    
    &:hover {
      transform: translateY(0px) scale(1);
      border-radius: 3.5rem;
    }
  }

  .arrowContainer {
    top: -0.5rem;
    right: -0.5rem;
  }
  
  .title {
    font-size: 1.4rem;
  }
  
  .subtitle {
    &.medium {
      font-size: 0.75rem;
      padding: 3px 8px;
    }
  }
  
  .description {
    font-size: 1rem;
    line-height: 1.5;
  }
  
  .ctaButton {
    font-size: 0.875rem;
    padding: 0.5rem 1rem;
  }
}

________________________________________________________________________________

АБСОЛЮТНЫЙ ПУТЬ: /Users/vladimirmazyrec/sync-agency-new/src/shared/sections/ProductHeroSection/ProductHeroSection.tsx
название файла: ProductHeroSection/ProductHeroSection.tsx

СОДЕРЖИМОЕ ФАЙЛА:
----------------------------------------
import React, { useState, useMemo, useRef, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { gsap } from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
import { HeroBackground, HeroBackgroundType } from '@/shared/ui/hero-background';
import { Button } from '@/shared/ui/button';
import styles from './ProductHeroSection.module.scss';

// Регистрируем плагин ScrollTrigger
gsap.registerPlugin(ScrollTrigger);

interface ProductHeroSectionProps {
  title: string;
  subtitle: string;
  description: string;
  ctaText: string;
  onCtaClick?: () => void;
  backgroundType: HeroBackgroundType;
  className?: string;
  id?: string;
  // Новые пропсы для навигации
  navigateTo?: string;
  onClick?: () => void;
}

// Маппинг типов фона к цветовым схемам
const COLOR_SCHEMES: Record<HeroBackgroundType, string> = {
  brand: 'blue',
  marketing: 'red',
  product: 'green',
  data: 'cyan',
  automation: 'purple',
} as const;

export const ProductHeroSection: React.FC<ProductHeroSectionProps> = ({
  title,
  subtitle,
  description,
  ctaText,
  onCtaClick,
  backgroundType,
  className = '',
  id,
  navigateTo,
  onClick
}) => {
  const [isHovered, setIsHovered] = useState(false);
  const [isScrolledTriggered, setIsScrolledTriggered] = useState(false);
  const containerRef = useRef<HTMLElement>(null);
  const navigate = useNavigate();
  const isMobile = typeof window !== 'undefined' ? window.innerWidth <= 767 : false;

  // Получаем цветовую схему на основе типа фона
  const colorScheme = useMemo(() => COLOR_SCHEMES[backgroundType] || 'blue', [backgroundType]);

  // Определяем классы для subtitle
  const subtitleClasses = useMemo(() => {
    const baseClass = styles.subtitle;
    const sizeClass = styles.medium;
    
    const isActive = isHovered || isScrolledTriggered;
    const colorClass = isActive ? `${colorScheme}Color` : '';
    
    return `${baseClass} ${sizeClass} ${colorClass}`.trim();
  }, [colorScheme, isHovered, isScrolledTriggered]);

  // Обработчик клика по всему компоненту
  const handleContainerClick = () => {
    if (onClick) {
      onClick();
    } else if (navigateTo) {
      navigate(navigateTo);
    } else if (onCtaClick) {
      onCtaClick();
    }
  };

  // Обработчик клика по кнопке CTA
  const handleButtonClick = (e: React.MouseEvent) => {
    e.stopPropagation();
    if (onCtaClick) {
      onCtaClick();
    } else {
      handleContainerClick();
    }
  };

  // Эффект для ScrollTrigger
  useEffect(() => {
    if (!containerRef.current || typeof window === 'undefined') return;

    const container = containerRef.current;
    const mm = gsap.matchMedia();
    
    mm.add("(max-width: 767px)", () => {
      const trigger = ScrollTrigger.create({
        trigger: container,
        start: "top 50%",
        end: "bottom 50%",
        onEnter: () => {
          setIsScrolledTriggered(true);
          container.setAttribute('data-scrolled', 'true');
          container.setAttribute('data-active', 'true');
        },
        onEnterBack: () => {
          setIsScrolledTriggered(true);
          container.setAttribute('data-scrolled', 'true');
          container.setAttribute('data-active', 'true');
        },
        onLeave: () => {
          setIsScrolledTriggered(false);
          container.setAttribute('data-scrolled', 'false');
          container.setAttribute('data-active', 'false');
        },
        onLeaveBack: () => {
          setIsScrolledTriggered(false);
          container.setAttribute('data-scrolled', 'false');
          container.setAttribute('data-active', 'false');
        },
        scrub: 0.5,
        toggleActions: "play none none reverse",
        markers: false,
      });

      return () => {
        trigger.kill();
      };
    });

    return () => {
      mm.revert();
    };
  }, []);

  const isActive = isHovered || isScrolledTriggered;

  return (
    <section
      ref={containerRef}
      className={`${styles.container} ${className}`}
      onMouseEnter={() => !isMobile && setIsHovered(true)}
      onMouseLeave={() => !isMobile && setIsHovered(false)}
      onTouchStart={() => isMobile && setIsHovered(true)}
      onTouchEnd={() => isMobile && setTimeout(() => setIsHovered(false), 100)}
      onClick={handleContainerClick}
      data-type={backgroundType}
      data-color-scheme={colorScheme}
      data-hovered={isHovered}
      data-scrolled={isScrolledTriggered}
      data-active={isActive}
      id={id}
      style={{ cursor: 'pointer' }}
    >
      <div className={styles.backgroundWrapper}>
        <HeroBackground 
          type={backgroundType} 
          speed={1}
          intensity={1}
          isHovered={isHovered}
          isActive={isActive}
          isScrolled={isScrolledTriggered}
        />
      </div>

      <div className={styles.content}>
        <div className={styles.header}>
          <div className={styles.headerTitle}>
            <span className={subtitleClasses}>{subtitle}</span>
            <div className={`${styles.arrowContainer} ${isActive ? styles.arrowVisible : ''}`}>
              <div className={styles.arrow}>
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M0 14.0904L0 10.6205H16.3636C15.2253 9.55926 14.6453 8.92032 13.3636 6.86145C12.4743 5.43281 12 3.10241 11.7273 1.65663L14.7273 0.5C15.4545 2.33133 15.7727 4.54819 18.2727 7.72892C19.8636 9.75301 21.8182 10.3313 24 10.6205V14.0904C21.5455 14.3795 19.5415 15.1087 17.7273 17.8494C15.913 20.5901 15.1818 22.9578 14.7273 24.5L11.7273 23.0542C12.1818 21.8976 12.664 19.9352 13.6364 18.1386C14.6087 16.3419 15.6364 14.9578 16.3636 14.0904H0Z"/>
                </svg>
              </div>
            </div>
          </div>

          <h1 className={styles.title}>{title}</h1>
        </div>
        
        <div className={`${styles.description} ${isActive ? styles.descriptionVisible : ''}`}>
          {description}
        </div>

      </div>
    </section>
  );
};

================================================================================

СТАТИСТИКА:
----------------------------------------
Обработано файлов: 3
Общий размер папки: 12,785 байт
Дата создания отчета: 2026-01-22 08:23:49
